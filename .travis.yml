language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-nsdcss' ]; then cd .. && eval "mv $currentfolder generator-nsdcss" && cd generator-nsdcss; fi

